<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Priorities.Views.PlayersPage"
             Title="PlayersPage"
             xmlns:vm="clr-namespace:Priorities.ViewModels"
             >
    <ScrollView>
        <VerticalStackLayout>

            <Label
            Text="Priorities"
            TextColor="Black"
            HorizontalTextAlignment="Center"
            FontSize="32"
            BackgroundColor="white"
            HorizontalOptions="Fill"/>

            <Label
            Margin="15"
            TextColor="White"
            Text="Players"
            FontSize="18"
            HorizontalOptions="Center" />


            <Grid BackgroundColor="White" Padding="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Label Grid.Row="0" Grid.Column="1" Text=" Player Image "  TextColor="Black" FontSize="18"/>
                <Label Grid.Row="0" Grid.Column="3" Text=" Player Name "   TextColor="Black" FontSize="18"/>
                <Label Grid.Row="0" Grid.Column="2" Text=" Remove Button " TextColor="Black" FontSize="18"/>

            </Grid>




            <Button
        Margin="25"
        TextColor="Black"
        Text="Add New Player"
        HorizontalOptions="Center" />

            <Button
            TextColor="Black"
            Text="Play"
            HorizontalOptions="Center" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
